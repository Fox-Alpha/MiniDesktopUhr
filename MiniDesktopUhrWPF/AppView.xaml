<UserControl x:Class="MiniDesktopUhrWPF.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniDesktopUhrWPF"
			 xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400">
	<Grid Width="386" Height="99" Background="LightBlue">
		<Grid.ContextMenu>
			<ContextMenu>
				<MenuItem Header="Datum anzeigen" IsChecked="{Binding bShowDate, Mode=TwoWay}" cal:Message.Attach="[Event Click] = [Action ShowDate()]"></MenuItem>
				<MenuItem Header="Beenden" cal:Message.Attach="[Event Click] = [Action AppExit()]"></MenuItem>
				
			</ContextMenu>
		</Grid.ContextMenu>
		<Border>
			<TextBlock x:Name="StrUhrzeit" Margin="5" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Center"/>
		</Border>
	</Grid>

</UserControl>
