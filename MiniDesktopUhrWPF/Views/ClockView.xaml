<UserControl x:Class="MiniDesktopUhrWPF.Views.ClockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MiniDesktopUhrWPF.Views"
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" Background="LightBlue"
             d:DesignHeight="100" d:DesignWidth="400"
             Height="100" Width="400">
    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Datum anzeigen" IsChecked="{Binding ShowDate, Mode=TwoWay}" cal:Message.Attach="[Event Click] = [Action ShowStringDate()]"/>
                <MenuItem Header="Beenden" cal:Message.Attach="[Event Click] = [Action AppExit()]"/>
                <Separator/>
                <MenuItem Header="Monitore" />
                <MenuItem Header="Einstellungen" cal:Message.Attach="[Event Click] = [Action OpenOptions()]"/>
                <Separator/>
            </ContextMenu>
        </Grid.ContextMenu>
        <Border>
            <TextBlock x:Name="StrUhrzeit" Margin="5" FontSize="25" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
